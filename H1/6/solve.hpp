#pragma once

#include <iostream>
#include <cstdint>
#include <vector>
#include <algorithm>
#include "other_func.hpp"

// g++ test.cpp -lgtest -lgtest_main -lpthread -O3 && clear && ./a.out

std::vector <std::string> solve (std::string set) {
    std::vector <std::string> subsets;

    
}